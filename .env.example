# =============================================================================
# Root env for docker-compose.yml (MySQL 8)
# Copy to .env and adjust before deployment:  cp .env.example .env
# =============================================================================

MYSQL_PORT=3306

MYSQL_DATABASE=dexa_wfh

MYSQL_USER=dexa
MYSQL_PASSWORD=dexa_password

MYSQL_ROOT_PASSWORD=rootpassword