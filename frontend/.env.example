# Frontend environment. Copy to .env: cp .env.example .env
# Backend API base (NestJS global prefix /api/v1)
VITE_API_BASE_URL=http://localhost:3000/api/v1
# Backend origin (for serving uploaded photos at /uploads)
VITE_API_ORIGIN=http://localhost:3000
