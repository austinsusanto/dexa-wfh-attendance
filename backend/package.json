{
	"name": "@dexa/backend",
	"version": "1.0.0",
	"description": "Dexa WFH backend — microservices workspace (gateway + identity + employees + attendances + shared common)",
	"private": true,
	"workspaces": [
		"common",
		"gateway",
		"identity",
		"employees",
		"attendances"
	],
	"scripts": {
		"build:common": "npm run build -w @dexa/common",
		"build": "npm run build:common && npm run build -w @dexa/gateway -w @dexa/identity -w @dexa/employees -w @dexa/attendances",
		"format": "prettier --write \"{common,gateway,identity,employees,attendances}/src/**/*.ts\"",
		"test": "npm run test -w @dexa/identity -w @dexa/employees -w @dexa/attendances -w @dexa/gateway"
	},
	"devDependencies": {
		"prettier": "^3.4.2"
	}
}
